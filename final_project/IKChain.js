export class IKChain {
    // an IK chain is of strucuture joint -> bone -> joint -> bone -> joint -> bone -> joint
    // so we will pass bone[i] in joint[i-1]
    constructor(joints, options = {}) {

    }
}